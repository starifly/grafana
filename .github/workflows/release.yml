name: Build and Release Multi-Arch

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string
      draft:
        description: 'Draft release'
        required: false
        default: false
        type: boolean
      prerelease:
        description: 'Pre-release'
        required: false
        default: false
        type: boolean

jobs:
  build:
    strategy:
      matrix:
        include:
          - arch: amd64
            runs-on: ubuntu-latest
          - arch: arm64
            runs-on: ubuntu-24.04-arm
    runs-on: ${{ matrix.runs-on }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential
      - name: Build fronted and backend
        run: |
          export GOOS=linux
          export GOARCH=${{ matrix.arch }}
          make deps-js
          make build-frontend
          make build-backend
      - name: Package binary
        run: |
          mkdir -p artifacts
          tar czf artifacts/grafana-${{ github.event.inputs.tag }}-linux-${{ matrix.arch }}.tar.gz \
            -C bin/linux/${{ matrix.arch }}/ grafana
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: grafana-linux-${{ matrix.arch }}
          path: artifacts/grafana-${{ github.event.inputs.tag }}-linux-${{ matrix.arch }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: Release ${{ github.event.inputs.tag }}
          draft: ${{ github.event.inputs.draft }}
          prerelease: ${{ github.event.inputs.prerelease }}
          files: artifacts/**/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
